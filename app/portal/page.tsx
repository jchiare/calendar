"use client";
import { AppShell, StatusBadge } from "@/components/app-shell";
import { useState } from "react";
export default function PortalPage(){const [message,setMessage]=useState("");async function saveIntake(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const values=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/clients/11111111-1111-4111-8111-111111111111/intake",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({concerns:values.concerns,homeLanguages:values.homeLanguages,consent:values.consent==="on"})});const result=await response.json();setMessage(response.ok?"Intake saved":result.error??"Could not save intake");}return <AppShell title="Mia's care" eyebrow="PARENT PORTAL"><div className="portal-grid"><section className="data-card section-card"><div className="section-title"><div><h2>Upcoming appointments</h2><p>With Alex Johnson</p></div><button className="primary">Book a time</button></div>{["Wed, Sep 17 · 9:00 AM","Wed, Sep 24 · 9:00 AM","Wed, Oct 1 · 9:00 AM"].map((date,i)=><div className="portal-appointment" key={date}><div className="date-tile"><b>{17+i*7}</b><small>SEP</small></div><div><strong>{date}</strong><p>Speech therapy · 45 minutes</p></div><StatusBadge>Scheduled</StatusBadge><button>Cancel</button></div>)}</section><aside className="data-card intake-card"><StatusBadge tone="orange">Action needed</StatusBadge><h2>Finish Mia&apos;s intake</h2><p>Tell the therapist what matters to your family. You can upload an IEP from Mia&apos;s profile.</p><form onSubmit={saveIntake}><label>Your concerns<textarea name="concerns" required placeholder="What would you most like help with?"/></label><label>Languages at home<input name="homeLanguages" required placeholder="English"/></label><label className="consent"><input name="consent" type="checkbox" required/> I consent to care-team use of these records.</label>{message&&<p aria-live="polite">{message}</p>}<button className="primary wide">Save intake</button></form></aside></div><section className="data-card section-card summaries"><div className="section-title"><div><h2>Session updates</h2><p>Notes your therapist has reviewed and shared</p></div></div><article><b>September 8</b><h3>Great work with speech sounds today</h3><p>Mia stayed engaged while practicing /r/ in familiar words. Visual placement reminders helped her get started, and she needed fewer cues as the activity continued.</p><strong>Try at home</strong><p>Pick five “r” words during dinner and say each one slowly together.</p></article></section></AppShell>}
